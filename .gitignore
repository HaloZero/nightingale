target/
.env

# Claude Code local session state (settings.local.json, worktrees, etc.)
.claude/

# Site
site/node_modules/
site/dist/
site/docs/book/
site/.astro/

# Local updater manifest staged before publishing
latest.json

# Local-only release/updater notes (not for the public repo)
RELEASING.md

# mac only
**.DS_STORE

**/__pycache__

songs*

# Analyzer benchmark output (scripts/bench_analyze.py)
bench_out/